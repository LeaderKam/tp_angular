import { PokeSearchPipe } from './poke-search.pipe';

describe('PokeSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PokeSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
